Gymlog source SVG icon set.

These files are kept as editable source assets. For React Native screens, prefer converting the needed icons into `react-native-svg` components or the shared `GymlogIcon` component instead of importing raw SVG files directly.
